experiment
dist
node_modules
devAssets

.DS_Store

# Android build artifacts (don't commit 119MB APKs or signing keys)
*.apk
*.apk.idsig
# ALL keystores, not just debug.keystore — a release keystore
# (prinny-ci.keystore) was committed to this public repo once already.
*.keystore
*.jks

# Tauri updater minisign keypair + password — NEVER commit
.secrets/

# Local smoke-test output of scripts/release-notes.mjs (CI writes it fresh each run)
release-notes.md
