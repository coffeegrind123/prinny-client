{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile-capability",
  "description": "Mobile-only permissions. Narrowed to what the frontend calls: fs, shell, dialog, http and clipboard were granted but never used, and each was reachable by any script executing in the page.",
  "platforms": [
    "iOS",
    "android"
  ],
  "permissions": [
    "core:default",
    "notification:default",
    "os:default",
    "process:default",
    "mobile-push:default",
    "deep-link:default",
    "deep-link:allow-get-current"
  ]
}
